/**
 * Function which returns `void`.
 */
export type VoidFn = () => void;

/**
 * Function which returns a value.
 */
export type Fn = (...args: unknown[]) => unknown;

/**
 * Any function which returns a value.
 */
export type AnyFn = (...args: unknown[]) => unknown;
